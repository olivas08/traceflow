apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-service
  namespace: traceflow
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catalog-service
  template:
    metadata:
      labels:
        app: catalog-service
    spec:
      containers:
        - name: catalog-service
          image: ghcr.io/traceflow/catalog-service:1.0.0
          ports:
            - containerPort: 8081
          env:
            - name: QUARKUS_HTTP_PORT
              value: "8081"
            - name: QUARKUS_DATASOURCE_JDBC_URL
              value: jdbc:postgresql://postgres:5432/catalog
            - name: QUARKUS_DATASOURCE_USERNAME
              value: traceflow
            - name: QUARKUS_DATASOURCE_PASSWORD
              value: traceflow
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: kafka:9092
          readinessProbe:
            httpGet:
              path: /q/health/ready
              port: 8081
            initialDelaySeconds: 20
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: traceflow
spec:
  selector:
    app: catalog-service
  ports:
    - port: 8081
      targetPort: 8081
