apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: traceflow
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
        - name: notification-service
          image: ghcr.io/traceflow/notification-service:1.0.0
          ports:
            - containerPort: 8083
          env:
            - name: QUARKUS_HTTP_PORT
              value: "8083"
            - name: QUARKUS_DATASOURCE_JDBC_URL
              value: jdbc:postgresql://postgres:5432/notification
            - name: QUARKUS_DATASOURCE_USERNAME
              value: traceflow
            - name: QUARKUS_DATASOURCE_PASSWORD
              value: traceflow
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: kafka:9092
          readinessProbe:
            httpGet:
              path: /q/health/ready
              port: 8083
            initialDelaySeconds: 20
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: traceflow
spec:
  selector:
    app: notification-service
  ports:
    - port: 8083
      targetPort: 8083
